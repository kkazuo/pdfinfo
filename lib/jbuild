(library
 ((name pdfinfo)
  (public_name pdfinfo)
  (libraries (base stdio ppx_pipebang ppx_sexp_conv))
  (flags (:standard))
  (preprocess (pps (ppx_pipebang ppx_sexp_conv ppx_driver.runner)))))

(jbuild_version 1)
